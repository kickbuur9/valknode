apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  # labels:
  #   app: elasticsearch
spec:
  type: NodePort
  selector:
    component: elasticsearch
  ports:
    - port: 9200
      targetPort: 9200
      nodePort: 31920
      name: http
    - port: 9300
      targetPort: 9300
      name: transport
